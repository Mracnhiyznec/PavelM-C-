#include <iostream>
using namespace std;
int main(){
    long long n; # заводим переменную типа long long так как числа могут быть очень большими
    cin >> n; # вводим число
    vector<long long> v; # объявляем вектор в котором будут наши делители
    for (long long i = 1; i * i <= n; i++) { # идем до корня
        if (n % i == 0) { # если число делитель добавляем его в вектор
            v.push_back(i);
            if (n / i != i) { # но тогда n / i тоже делитель и больше корня из n => два одиннаковых числа добавлены не будут, единстенное исключение для числа которое в квадрате дает n
                v.push_back(n / i);
            }
        }
    }
    sort(v.begin(), v.end()); # сортируем массив так как наши делители были добавленны в разнобой
    for (long long x:v){
        cout << x << "\n"; # выводим числа через пробел
    }
    return 0;
}
